<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Wallpaper bitmap is set at runtime; white fallback matches the light native theme. -->
    <ImageView
        android:id="@+id/panel_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"/>

    <!-- Replica of native notifyInfo: centered envelope glyph + single-line sender text. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="2dp"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/panel_icon"
            android:layout_width="12dp"
            android:layout_height="12dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_sms"
            android:tint="#FFFFFFFF"/>

        <TextView
            android:id="@+id/panel_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:paddingLeft="2dp"
            android:paddingRight="2dp"
            android:textColor="#FFFFFFFF"
            android:textSize="13px"
            android:singleLine="true"
            android:ellipsize="end"/>

        <!--
          Message preview. Hidden at runtime when the notification carries no body, which keeps
          the sender-only case pixel-identical to the native banner. Sizes are px on purpose:
          the panel is inflated with display 0's metrics (240 dpi) but drawn on a 128 px screen,
          so sp/dp would come out ~12% too large.
        -->
        <TextView
            android:id="@+id/panel_preview"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:paddingLeft="3dp"
            android:paddingRight="3dp"
            android:textColor="#FFC8C8C8"
            android:textSize="11px"
            android:maxLines="2"
            android:ellipsize="end"/>
    </LinearLayout>

</FrameLayout>
